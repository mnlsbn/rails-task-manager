<h2><%= @task.name %></h2>
<p><%= @task.details %></p>
<p><%= @task.updated_at %></p>

<%= link_to "Delete", task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>

<%= link_to "Edit", edit_task_path(@task) %>

<%= link_to "Index", "/tasks" %>
